# 任务：自主分析并优化排水管网缺陷定位模型

## 你的角色
你是一个自主调试Agent。你需要自己读代码、运行脚本、分析结果、提出假设、验证假设、修改代码，形成完整的调试闭环。不要等待用户指令，主动推进。

## 项目目标
基于时空图神经网络（GRU+GCN）对城市排水管网进行缺陷定位。
- 输入：128个节点的时序特征（水位、流量、污染物浓度及其残差）
- 输出：50个候选节点中哪个发生了缺陷（I类渗入/E类渗漏/P类点源污染）
- 评估：MRR、Top-1/3/5召回率

## 当前结果（不理想）
- 测试集 MRR≈0.23，Top-1≈0.15
- 训练loss持续下降但验证loss持续上涨（过拟合）
- 预测时反复输出固定几个节点（模型坍缩）
- 不同场景间性能差异悬殊

## 项目路径
```
E:\11.16\script2\          # 所有脚本
E:\11.16\input2\           # 缺陷矩阵、邻接矩阵、候选节点
E:\11.16\training_data_2\  # 训练数据parquet文件
E:\11.16\training_data_time_gated\
E:\11.16\input_data\       # SWMM inp管网文件
```

## 你的任务

### 第一阶段：诊断
1. 读取所有关键脚本，理解完整数据流：仿真→提取→特征工程→数据集构建→训练→评估
2. 读取训练数据，分析数据质量（信号强度分布、标签分布、场景间差异）
3. 运行 `26_check_labels.py`，分析输出
4. 运行一次完整训练，仔细分析训练日志
5. 形成你自己的问题诊断报告，列出你认为影响性能的所有问题，并按影响程度排序

### 第二阶段：验证假设
针对你诊断出的每个问题，设计最小化验证实验：
- 一次只改一个变量
- 每次实验前记录当前baseline
- 运行后对比结果，确认假设是否成立

### 第三阶段：迭代优化
根据验证结果，从收益最高、成本最低的问题开始修复，逐步迭代，每轮记录MRR变化。

## 约束条件
- PySWMM仿真可以直接运行，单次仿真<1分钟
- 当前数据约1GB内存，增加场景数前需先评估内存影响
- 每次修改代码前先备份原文件
- 实验结果用表格记录，方便对比

## 期望输出
每完成一个阶段，输出：
1. 你发现了什么问题
2. 你做了什么实验来验证
3. 实验结果是什么
4. 下一步计划是什么

**不要只报告结论，要展示你的推理过程。**
